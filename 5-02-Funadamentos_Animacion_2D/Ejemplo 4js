function setup() { 
    createCanvas(600, 400); 
} 

function draw() { 
    background(30); 
    let s = 1 + sin(frameCount * 0.05); 
    
    push(); 

    translate(300, 200); 

    scale(s); 

    fill(255, 100, 100); 
    rectMode(CENTER); 
    rect(0, 0, 100, 100); 

    pop(); 
}